<%= form_with(model: band_member, local: true) do |form| %>
  <% if band_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(band_member.errors.count, "error") %> prohibited this band_member from being saved:</h2>

      <ul>
      <% band_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :instrument %>
    <%= form.text_field :instrument %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
